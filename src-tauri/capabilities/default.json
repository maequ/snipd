{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window, the capture overlay and the editor.",
  "windows": ["main", "overlay", "editor"],
  "permissions": ["core:default", "opener:default"]
}
